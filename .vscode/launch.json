{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Windows)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\${fileBasenameNoExtension}.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "logging": {
                "moduleLoad": false,
                "trace": true
            },
        }
    ]
}